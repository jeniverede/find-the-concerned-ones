import "../styles/BottomMenu.css";

export function BottomMenu() {
    /* create a function here to enable the menu coming up from bottom of screen
    The text "Districts" should be always showing at the bottom of the screen to enable the menu */
  return (
    <>
      <div className="home-bottom-menu-container">
        <h3>Districts</h3>
        {/* create a map here to display all the districts, create a json file?
        (ex: Berlin-Mitte or Berlin Mitte)
        add icon rightarrow to the right of the text */}
      </div>
    </>
  );
}
